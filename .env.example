# Published builder image family.
# Per-platform tags are derived as:
#   ${BUILDER_IMAGE_FAMILY}-centos7
#   ${BUILDER_IMAGE_FAMILY}-rocky8
#   ${BUILDER_IMAGE_FAMILY}-ubuntu2204
# The default family format is:
#   tf-particles/devops/cuda-builder:cuda${BUILDER_CUDA_VERSION}-cmake3.26
# The default tag format is:
#   tf-particles/devops/cuda-builder:cuda${BUILDER_CUDA_VERSION}-cmake3.26-${BUILDER_DEFAULT_PLATFORM}
BUILDER_CUDA_VERSION=11.7.1

# Default platform used by build scripts when no explicit --platform is given.
BUILDER_DEFAULT_PLATFORM=centos7

# Comma-separated platform matrix used by the builder image build script.
BUILDER_PLATFORMS=centos7,rocky8,ubuntu2204
